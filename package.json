{
  "name": "residanat-lockdown",
  "version": "1.0.0",
  "description": "Study-arcade command center for the Algerian residanat exam.",
  "type": "module",
  "engines": { "node": ">=22.5.0" },
  "scripts": {
    "dev:server": "node --watch server/index.js",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client install && npm --prefix client run build",
    "start": "node server/index.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.1"
  }
}
