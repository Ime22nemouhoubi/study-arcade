# Copy to .env for local dev. On Railway, set these as Variables.
JWT_SECRET=change-me-to-a-long-random-string
# On Railway: attach a Volume mounted at /data, then set:
# DATABASE_PATH=/data/residanat.db
DATABASE_PATH=./server/data/residanat.db
NODE_ENV=development
PORT=3000
